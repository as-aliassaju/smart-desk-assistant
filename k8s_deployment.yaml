apiVersion: apps/v1
kind: Deployment
metadata:
  name: vue-python-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: vue-python-app
  template:
    metadata:
      labels:
        app: vue-python-app
    spec:
      containers:
      - name: vue-python-container
        image: us-central1-docker.pkg.dev/ci-cd-pipeline-implementation/aliassajucicdproject/vue-python-ci-cd-appliation:{{IMAGE_TAG}}
        imagePullPolicy: Always
        ports:
        - containerPort: 5000

---
apiVersion: v1
kind: Service
metadata:
  name: vue-python-lb
spec:
  selector:
    app: vue-python-app
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000